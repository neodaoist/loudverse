# Close a funding round, compute quadratic funding and apply grant match

name: Close Funding Round

# Run Manually
on:
  workflow_dispatch:
    # Inputs the workflow accepts.
    inputs:
      matchpool_address:
        # Friendly description to be shown in the UI instead of 'name'
        description: 'Matchpool address'
        # Default value if no value is explicitly provided
        default: ''
        # Input has to be provided for the workflow to run
        required: true


# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "greet"
  funding:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Runs a single command using the runners shell
    - name: Set up ts-node
      run: npm install -g ts-node
    - name: Computed Double-Constrained Quadratic Funding
      run: node --loader ts-node/esm endroundhook.ts ${{ github.event.inputs.matchpool_address }}

