type Grant @entity {
  # address of the proxy
  id: ID!

  creator: User!

  title: String!
  #...
}

type User @entity {
  # address of the user
  id: ID!

  grants: [Grant!]! @derivedFrom(field: "creator")
}
